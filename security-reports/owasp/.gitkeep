# OWASP Dependency-Check Reports

This directory stores OWASP Dependency-Check vulnerability reports.

## Running Dependency Check

```bash
cd product-service && ./mvnw dependency-check:check
cd order-service && ./mvnw dependency-check:check
cd gateway && ./mvnw dependency-check:check
```

## Report Location

Reports are generated in each service's `target/` directory:
- `target/dependency-check-report.html`
- `target/dependency-check-report.json`
